import numpy as np
import matplotlib.pyplot as pl
import glob


daysdata = []
for day in days:
	daysdata.append(np.loadtxt(day,delimiter=",",skiprows=1,usecols=(1,2)))

d=0
for d in range(0,len(daysdata))
	pl.plot([daysdata[d][:,0]],daysdata[d][:,0],"r--*")



#np.max(dataarray[:,0])
#np.min(dataarray[:,0])
#np.mean(dataarray[:,0])

days+"i" = np.loadtxt("*day.csv",delimiter=",",skiprows=1)
 i=i+1


q=0
for q in days
	q=q+1


dataarray=np.loadtxt(days,delimiter=",",skiprows=1,usecols=(1,2))



days=glob.glob("*day.csv")
datadays=np.loadtxt(days,delimiter=",",skiprows=1,usecols=(1,2)
pl.plot([days[0][:,0]],data1[:,1],"r--*")

empty = ""
for i in 
	
	i=i+1
	"data"+i 
